#ifndef MEASURE_HEADER
#define MEASURE_HEADER

// Function to measure integer calculations
long measure_int (int type);

// Function to measure long calculations
long measure_long (int type);

// Function to measure 32-bit float calculations
long measure_32f (int type);

// Function to measure 64-bit float (double) calculations
long measure_double (int type);

#endif